using UnityEngine;

public class PlayerCamera : MonoBehaviour
{
    // Get the position of the playerModel
    public Transform cameraPos;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
